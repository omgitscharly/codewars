//two numbers that are angles of a triangle

//return a number that is th third angle

function otherAngle(a, b) {
    //return the differrence beween a + b and 180
    return 180 - (a + b);
  }
  
  otherAngle(10, 20)
  
  // (30, 60), 90
  // (60, 60), 60
  // (43, 78), 59
  // (10, 20), 150